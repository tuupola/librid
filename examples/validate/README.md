# Validate Example

```
$ make run
```

## Output

```
Valid Serial Number:   RID_SUCCESS
Invalid Serial Number: RID_ERROR_INVALID_CHARACTER
Valid UTM UUID:        RID_SUCCESS
Invalid UTM UUID:      RID_ERROR_INVALID_UUID_VERSION
```
